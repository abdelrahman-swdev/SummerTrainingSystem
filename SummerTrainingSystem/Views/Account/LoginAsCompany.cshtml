@model LoginAsCompanyVM
@{
    ViewData["Title"] = "Login As Company";
}
<div class="container my-5">
    <div class="row">
        <div class="col-lg-6 mb-5">
            <h1 class="display-5">Login As Company</h1>
            <hr />
            @if (!String.IsNullOrEmpty(ViewBag.LoginFailed))
            {
                <div class="alert alert-danger">
                    @ViewBag.LoginFailed
                </div>
            }
            <form method="post">
                <div class="mt-4">
                    <input class="form-control" asp-for="Email" placeholder="Email Address" />
                    <span class="text-danger" asp-validation-for="Email"></span>
                </div>
                <div class="mt-4">
                    <input class="form-control" asp-for="Password" placeholder="Password" />
                    <span class="text-danger" asp-validation-for="Password"></span>
                </div>
                <div class="mt-4 d-flex justify-content-between">
                    <button class="btn btn-success">Login</button>
                    <a class="btn btn-outline-dark" asp-controller="Home" asp-action="Index">Back</a>
                </div>
            </form>
            <div class="mt-4 text-danger" asp-validation-summary="ModelOnly"></div>
        </div>
        <div class="col-md-5 offset-md-1">
            <img src="~/images/register.svg" alt="student" style="max-width: 100%;"/>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}